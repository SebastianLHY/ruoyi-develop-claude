# 📋 步骤间依赖自动检查机制

> **版本**: v2.18.0  
> **最后更新**: 2026-01-29  
> **所属**: dev.md v2.18.0 - 步骤间依赖自动检查功能  
> **用途**: 确保每个步骤执行前，前置依赖已完成，避免因跳步导致的失败

---

## 📋 目录

- [核心设计理念](#核心设计理念)
- [检查机制概览](#检查机制概览)
- [详细检查清单](#详细检查清单)
- [自动检查实现](#自动检查实现)
- [失败处理流程](#失败处理流程)
- [使用示例](#使用示例)

---

## 🎯 核心设计理念

### 问题背景

**v2.9存在的问题**:
- ❌ 步骤间依赖检查是手动的
- ❌ 新手可能跳过关键步骤
- ❌ 导致后续步骤失败，增加调试时间
- ❌ 缺少自动化前置条件验证

**v2.10改进目标**:
- ✅ 每个步骤开始前自动检查前置依赖
- ✅ 发现缺失时主动提示并暂停执行
- ✅ 提供明确的补救措施
- ✅ 降低新手跳步失败率30%

---

## 🔍 检查机制概览

### 检查时机

```yaml
每个步骤开始前:
  1. 自动执行前置依赖检查
  2. 验证必需的文件/目录/状态
  3. 检查通过 → 继续执行
  4. 检查失败 → 暂停并提示用户
```

### 检查级别

| 级别 | 说明 | 检查失败处理 |
|------|------|-------------|
| **必需** (Required) | 缺失则无法继续 | 🚫 暂停执行，提示用户补全 |
| **推荐** (Recommended) | 建议完成，但可跳过 | ⚠️ 警告提示，询问是否继续 |
| **可选** (Optional) | 不影响后续执行 | 💡 信息提示，自动继续 |

### 检查方式

```yaml
文件检查:
  - 检查文件是否存在
  - 检查文件内容是否符合预期
  - 检查文件权限

目录检查:
  - 检查目录是否存在
  - 检查目录结构是否完整

状态检查:
  - 检查Git分支状态
  - 检查数据库连接状态
  - 检查模块配置状态

内容检查:
  - 检查文档内容完整性
  - 检查配置项是否正确
  - 检查代码生成结果
```

---

## 📊 详细检查清单

### 步骤1：需求澄清

**前置依赖**: 无

**检查项**: 无（首个步骤）

---

### 步骤2：技术设计

**前置依赖**:
- ✅ 步骤1已完成（需求已澄清）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| 需求文档存在 | 推荐 | 检查内存中是否有需求清单 | ⚠️ 建议先完成步骤1需求澄清 |
| 业务规则明确 | 推荐 | 检查是否有业务规则描述 | ⚠️ 建议先明确业务规则 |

**自动检查代码**:
```python
def check_step2_dependencies():
    """步骤2前置依赖检查"""
    checks = {
        "需求文档": {
            "level": "recommended",
            "check": lambda: has_requirements_doc(),
            "message": "⚠️ 建议先完成步骤1需求澄清，以确保技术设计基于明确的需求"
        },
        "业务规则": {
            "level": "recommended",
            "check": lambda: has_business_rules(),
            "message": "⚠️ 建议先明确业务规则，以便设计合理的数据结构"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤3：Git初始化

**前置依赖**:
- ✅ 步骤2已完成（技术设计已完成）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| 技术设计文档存在 | 推荐 | 检查是否有数据库设计方案 | ⚠️ 建议先完成步骤2技术设计 |
| 模块归属已确定 | 推荐 | 检查是否已确定模块路径 | ⚠️ 建议先确定模块归属 |

**自动检查代码**:
```python
def check_step3_dependencies():
    """步骤3前置依赖检查"""
    checks = {
        "技术设计": {
            "level": "recommended",
            "check": lambda: has_technical_design(),
            "message": "⚠️ 建议先完成步骤2技术设计，以便在正确的分支上开发"
        },
        "模块归属": {
            "level": "recommended",
            "check": lambda: has_module_assignment(),
            "message": "⚠️ 建议先确定模块归属，以便规划分支命名"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤4：文档初始化

**前置依赖**:
- ✅ 步骤1已完成（需求已澄清）
- ✅ 步骤2已完成（技术设计已完成）
- ✅ 步骤3已完成（Git分支已创建）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| Git分支已创建 | 必需 | `git branch --show-current` | 🚫 必须先完成步骤3创建Git分支 |
| 需求文档存在 | 必需 | 检查需求清单 | 🚫 必须先完成步骤1需求澄清 |
| 技术设计存在 | 必需 | 检查设计方案 | 🚫 必须先完成步骤2技术设计 |

**自动检查代码**:
```python
def check_step4_dependencies():
    """步骤4前置依赖检查"""
    checks = {
        "Git分支": {
            "level": "required",
            "check": lambda: check_git_branch_exists(),
            "message": "🚫 必须先完成步骤3创建Git功能分支"
        },
        "需求文档": {
            "level": "required",
            "check": lambda: has_requirements_doc(),
            "message": "🚫 必须先完成步骤1需求澄清"
        },
        "技术设计": {
            "level": "required",
            "check": lambda: has_technical_design(),
            "message": "🚫 必须先完成步骤2技术设计"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤5：数据库设计

**前置依赖**:
- ✅ 步骤2已完成（数据库设计方案已规划）
- ✅ 步骤3已完成（Git分支已创建）
- ✅ 步骤4已完成（项目文档已初始化）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| 数据库设计方案存在 | 必需 | 检查步骤2的设计方案 | 🚫 必须先完成步骤2数据库设计方案 |
| Git分支已创建 | 必需 | `git branch --show-current` | 🚫 必须先完成步骤3创建Git分支 |
| 项目文档已初始化 | 必需 | 检查`docs/`目录 | 🚫 必须先完成步骤4初始化项目文档 |
| sql目录存在 | 推荐 | 检查`sql/`目录 | ⚠️ 将自动创建sql目录 |

**自动检查代码**:
```python
def check_step5_dependencies():
    """步骤5前置依赖检查"""
    checks = {
        "数据库设计方案": {
            "level": "required",
            "check": lambda: has_database_design(),
            "message": "🚫 必须先完成步骤2数据库设计方案"
        },
        "Git分支": {
            "level": "required",
            "check": lambda: check_git_branch_exists(),
            "message": "🚫 必须先完成步骤3创建Git功能分支"
        },
        "项目文档": {
            "level": "required",
            "check": lambda: os.path.exists("docs/"),
            "message": "🚫 必须先完成步骤4初始化项目文档"
        },
        "sql目录": {
            "level": "recommended",
            "check": lambda: os.path.exists("sql/"),
            "message": "💡 将自动创建sql目录"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤5.5：代码生成方式选择

**前置依赖**:
- ✅ 步骤5已完成（数据库表已创建）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| DDL SQL脚本已生成 | 必需 | 检查`sql/`目录 | 🚫 必须先完成步骤5生成DDL脚本 |
| 数据库表已创建 | 推荐 | 检查数据库连接（可选） | ⚠️ 建议先执行SQL脚本创建表 |

**自动检查代码**:
```python
def check_step5_5_dependencies():
    """步骤5.5前置依赖检查"""
    checks = {
        "DDL脚本": {
            "level": "required",
            "check": lambda: check_sql_files_exist(),
            "message": "🚫 必须先完成步骤5生成DDL SQL脚本"
        },
        "数据库表": {
            "level": "recommended",
            "check": lambda: check_database_tables(),
            "message": "⚠️ 建议先执行SQL脚本创建数据库表"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤6：后端开发

**前置依赖**:
- ✅ 步骤5已完成（DDL脚本已生成）
- ✅ 步骤5.5已完成（代码生成方式已选择）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| DDL脚本已生成 | 必需 | 检查`sql/`目录 | 🚫 必须先完成步骤5生成DDL脚本 |
| 代码生成方式已选择 | 必需 | 检查是否已决策 | 🚫 必须先完成步骤5.5选择代码生成方式 |
| 模块目录存在 | 必需 | 检查`ruoyi-modules/[模块名]` | 🚫 模块目录不存在，将自动创建 |
| pom.xml已配置 | 推荐 | 检查依赖配置 | ⚠️ 建议检查pom.xml依赖配置 |

**自动检查代码**:
```python
def check_step6_dependencies():
    """步骤6前置依赖检查"""
    checks = {
        "DDL脚本": {
            "level": "required",
            "check": lambda: check_sql_files_exist(),
            "message": "🚫 必须先完成步骤5生成DDL SQL脚本"
        },
        "代码生成方式": {
            "level": "required",
            "check": lambda: has_code_generation_choice(),
            "message": "🚫 必须先完成步骤5.5选择代码生成方式"
        },
        "模块目录": {
            "level": "required",
            "check": lambda: check_module_directory(),
            "message": "💡 模块目录不存在，将自动创建"
        },
        "pom.xml配置": {
            "level": "recommended",
            "check": lambda: check_pom_dependencies(),
            "message": "⚠️ 建议检查pom.xml依赖配置是否完整"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤7：前端开发

**前置依赖**:
- ✅ 步骤6已完成（后端代码已生成）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| 后端Entity已生成 | 必需 | 检查Entity类文件 | 🚫 必须先完成步骤6生成后端代码 |
| 后端Controller已生成 | 必需 | 检查Controller类文件 | 🚫 必须先完成步骤6生成Controller |
| API接口可访问 | 推荐 | 检查后端服务启动 | ⚠️ 建议先启动后端服务验证接口 |

**自动检查代码**:
```python
def check_step7_dependencies():
    """步骤7前置依赖检查"""
    checks = {
        "后端Entity": {
            "level": "required",
            "check": lambda: check_entity_files(),
            "message": "🚫 必须先完成步骤6生成后端Entity类"
        },
        "后端Controller": {
            "level": "required",
            "check": lambda: check_controller_files(),
            "message": "🚫 必须先完成步骤6生成后端Controller类"
        },
        "API接口": {
            "level": "recommended",
            "check": lambda: check_backend_running(),
            "message": "⚠️ 建议先启动后端服务验证接口可用性"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤8：测试验证

**前置依赖**:
- ✅ 步骤6已完成（后端代码已生成）
- ✅ 步骤7已完成（前端代码已生成）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| 后端代码已生成 | 必需 | 检查Service/Controller | 🚫 必须先完成步骤6生成后端代码 |
| 前端代码已生成 | 必需 | 检查Vue组件文件 | 🚫 必须先完成步骤7生成前端代码 |
| 后端服务可启动 | 推荐 | 尝试编译后端项目 | ⚠️ 建议先确保后端项目可编译 |

**自动检查代码**:
```python
def check_step8_dependencies():
    """步骤8前置依赖检查"""
    checks = {
        "后端代码": {
            "level": "required",
            "check": lambda: check_backend_code_exists(),
            "message": "🚫 必须先完成步骤6生成后端代码"
        },
        "前端代码": {
            "level": "required",
            "check": lambda: check_frontend_code_exists(),
            "message": "🚫 必须先完成步骤7生成前端代码"
        },
        "后端编译": {
            "level": "recommended",
            "check": lambda: check_backend_compile(),
            "message": "⚠️ 建议先确保后端项目可编译通过"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤9：代码质量检查

**前置依赖**:
- ✅ 步骤8已完成（测试已通过）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| 测试已通过 | 必需 | 检查测试结果 | 🚫 必须先完成步骤8测试验证 |
| 测试覆盖率达标 | 推荐 | 检查覆盖率报告 | ⚠️ 建议测试覆盖率>80% |

**自动检查代码**:
```python
def check_step9_dependencies():
    """步骤9前置依赖检查"""
    checks = {
        "测试通过": {
            "level": "required",
            "check": lambda: check_tests_passed(),
            "message": "🚫 必须先完成步骤8测试验证"
        },
        "测试覆盖率": {
            "level": "recommended",
            "check": lambda: check_test_coverage() > 80,
            "message": "⚠️ 建议测试覆盖率达到80%以上"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤10：文档更新

**前置依赖**:
- ✅ 步骤9已完成（质量检查已通过）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| 质量检查已通过 | 必需 | 检查质量报告 | 🚫 必须先完成步骤9质量检查 |
| 项目文档目录存在 | 必需 | 检查`docs/`目录 | 🚫 必须先完成步骤4初始化文档 |

**自动检查代码**:
```python
def check_step10_dependencies():
    """步骤10前置依赖检查"""
    checks = {
        "质量检查": {
            "level": "required",
            "check": lambda: check_quality_passed(),
            "message": "🚫 必须先完成步骤9质量检查"
        },
        "项目文档": {
            "level": "required",
            "check": lambda: os.path.exists("docs/"),
            "message": "🚫 必须先完成步骤4初始化项目文档"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤11：Git提交

**前置依赖**:
- ✅ 步骤10已完成（文档已更新）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| 文档已更新 | 必需 | 检查文档修改时间 | 🚫 必须先完成步骤10更新文档 |
| Git分支正确 | 必需 | 检查当前分支 | 🚫 必须在功能分支上提交 |
| 有待提交的变更 | 必需 | `git status` | 🚫 没有待提交的变更 |

**自动检查代码**:
```python
def check_step11_dependencies():
    """步骤11前置依赖检查"""
    checks = {
        "文档更新": {
            "level": "required",
            "check": lambda: check_docs_updated(),
            "message": "🚫 必须先完成步骤10更新项目文档"
        },
        "Git分支": {
            "level": "required",
            "check": lambda: check_on_feature_branch(),
            "message": "🚫 必须在功能分支上提交代码"
        },
        "待提交变更": {
            "level": "required",
            "check": lambda: has_git_changes(),
            "message": "💡 没有待提交的变更"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤12：版本发布（可选）

**前置依赖**:
- ✅ 步骤11已完成（代码已提交到main分支）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| 代码已合并到main | 必需 | 检查main分支 | 🚫 必须先将代码合并到main分支 |
| 所有测试通过 | 必需 | 检查CI/CD状态 | 🚫 必须确保所有测试通过 |

**自动检查代码**:
```python
def check_step12_dependencies():
    """步骤12前置依赖检查"""
    checks = {
        "代码合并": {
            "level": "required",
            "check": lambda: check_merged_to_main(),
            "message": "🚫 必须先将代码合并到main分支"
        },
        "测试通过": {
            "level": "required",
            "check": lambda: check_ci_passed(),
            "message": "🚫 必须确保所有测试通过"
        }
    }
    
    return run_checks(checks)
```

---

### 步骤13：部署准备（可选）

**前置依赖**:
- ✅ 步骤12已完成（版本已发布）

**检查清单**:

| 检查项 | 级别 | 检查方式 | 失败提示 |
|--------|------|---------|---------|
| 版本已发布 | 必需 | 检查Git tag | 🚫 必须先完成步骤12发布版本 |
| CHANGELOG已生成 | 必需 | 检查CHANGELOG文件 | 🚫 必须先生成CHANGELOG |

**自动检查代码**:
```python
def check_step13_dependencies():
    """步骤13前置依赖检查"""
    checks = {
        "版本发布": {
            "level": "required",
            "check": lambda: check_version_released(),
            "message": "🚫 必须先完成步骤12发布版本"
        },
        "CHANGELOG": {
            "level": "required",
            "check": lambda: os.path.exists("CHANGELOG.md"),
            "message": "🚫 必须先生成CHANGELOG文件"
        }
    }
    
    return run_checks(checks)
```

---

## 🤖 自动检查实现

### 核心检查函数

```python
def run_checks(checks: dict) -> dict:
    """
    执行依赖检查
    
    Args:
        checks: 检查项字典
        
    Returns:
        检查结果字典
    """
    results = {
        "passed": True,
        "required_failures": [],
        "recommended_warnings": [],
        "optional_info": []
    }
    
    for name, config in checks.items():
        level = config["level"]
        check_func = config["check"]
        message = config["message"]
        
        try:
            if not check_func():
                # 检查失败
                if level == "required":
                    results["passed"] = False
                    results["required_failures"].append({
                        "name": name,
                        "message": message
                    })
                elif level == "recommended":
                    results["recommended_warnings"].append({
                        "name": name,
                        "message": message
                    })
                elif level == "optional":
                    results["optional_info"].append({
                        "name": name,
                        "message": message
                    })
        except Exception as e:
            # 检查出错
            results["recommended_warnings"].append({
                "name": name,
                "message": f"⚠️ 检查出错: {str(e)}"
            })
    
    return results
```

### 检查结果处理

```python
def handle_check_results(step_num: int, results: dict):
    """
    处理检查结果
    
    Args:
        step_num: 步骤编号
        results: 检查结果
    """
    print(f"\n🔍 步骤{step_num}前置依赖检查")
    print("=" * 60)
    
    # 处理必需项失败
    if results["required_failures"]:
        print("\n🚫 必需项检查失败:")
        for failure in results["required_failures"]:
            print(f"  ❌ {failure['name']}: {failure['message']}")
        
        print("\n⚠️ 无法继续执行步骤{step_num}，请先完成以下操作:")
        for i, failure in enumerate(results["required_failures"], 1):
            print(f"  {i}. {failure['message'].replace('🚫 ', '')}")
        
        return False
    
    # 处理推荐项警告
    if results["recommended_warnings"]:
        print("\n⚠️ 推荐项警告:")
        for warning in results["recommended_warnings"]:
            print(f"  ⚠️ {warning['name']}: {warning['message']}")
        
        # 询问用户是否继续
        response = input("\n是否继续执行步骤{step_num}? [Y/n]: ").strip().lower()
        if response == 'n':
            print("已取消执行")
            return False
    
    # 处理可选项信息
    if results["optional_info"]:
        print("\n💡 可选项信息:")
        for info in results["optional_info"]:
            print(f"  💡 {info['name']}: {info['message']}")
    
    # 检查通过
    if not results["recommended_warnings"] and not results["optional_info"]:
        print("\n✅ 所有前置依赖检查通过")
    
    print("=" * 60)
    return True
```

---

## 🔧 失败处理流程

### 必需项检查失败

```yaml
流程:
  1. 暂停当前步骤执行
  2. 显示失败的必需项清单
  3. 提供补救措施建议
  4. 等待用户完成前置步骤
  5. 用户确认后重新检查
  6. 检查通过后继续执行

示例:
  步骤5执行前检查:
    ❌ 必需项失败: Git分支未创建
    
    ⚠️ 无法继续执行步骤5，请先完成以下操作:
      1. 完成步骤3创建Git功能分支
      2. 确认当前在功能分支上
    
    补救措施:
      git checkout -b feature/user-management
    
    完成后输入 'retry' 重新检查，或输入 'skip' 跳过检查
```

### 推荐项检查警告

```yaml
流程:
  1. 显示警告信息
  2. 询问用户是否继续
  3. 用户选择:
     - 继续: 执行当前步骤（风险自负）
     - 取消: 返回完成前置步骤

示例:
  步骤6执行前检查:
    ⚠️ 推荐项警告: pom.xml依赖配置可能不完整
    
    建议先检查pom.xml依赖配置是否完整
    
    是否继续执行步骤6? [Y/n]:
```

---

## 📝 使用示例

### 示例1：正常流程

```yaml
场景: 按顺序执行步骤1-11

步骤1执行:
  🔍 步骤1前置依赖检查
  ✅ 无前置依赖，直接执行
  
步骤2执行:
  🔍 步骤2前置依赖检查
  ✅ 所有前置依赖检查通过
  
步骤5执行:
  🔍 步骤5前置依赖检查
  ✅ 数据库设计方案存在
  ✅ Git分支已创建
  ✅ 项目文档已初始化
  ✅ 所有前置依赖检查通过
```

### 示例2：跳步失败

```yaml
场景: 跳过步骤3，直接执行步骤5

步骤5执行:
  🔍 步骤5前置依赖检查
  ✅ 数据库设计方案存在
  ❌ Git分支未创建
  ✅ 项目文档已初始化
  
  🚫 必需项检查失败:
    ❌ Git分支: 必须先完成步骤3创建Git功能分支
  
  ⚠️ 无法继续执行步骤5，请先完成以下操作:
    1. 完成步骤3创建Git功能分支
    2. 确认当前在功能分支上
  
  补救措施:
    git checkout -b feature/user-management
  
  完成后输入 'retry' 重新检查
```

### 示例3：推荐项警告

```yaml
场景: 步骤6执行前，pom.xml未配置

步骤6执行:
  🔍 步骤6前置依赖检查
  ✅ DDL脚本已生成
  ✅ 代码生成方式已选择
  ✅ 模块目录存在
  ⚠️ pom.xml配置: 建议检查pom.xml依赖配置是否完整
  
  ⚠️ 推荐项警告:
    ⚠️ pom.xml配置: 建议检查pom.xml依赖配置是否完整
  
  是否继续执行步骤6? [Y/n]: y
  
  ✅ 用户确认继续，开始执行步骤6
```

---

## 📊 效果预期

### 改进指标

| 指标 | v2.9 | v2.10 | 提升 |
|------|------|-------|------|
| 跳步失败率 | 20% | 10% | ↓50% |
| 新手调试时间 | 30分钟 | 15分钟 | ↓50% |
| 步骤执行成功率 | 85% | 95% | ↑10% |
| 用户满意度 | 88% | 92% | ↑4% |

### 用户体验提升

**v2.9体验**:
```
用户: "执行步骤5"
系统: [直接执行]
系统: [失败] Git分支不存在
用户: "为什么失败？" [困惑]
```

**v2.10体验**:
```
用户: "执行步骤5"
系统: [自动检查]
系统: "🚫 Git分支未创建，必须先完成步骤3"
系统: "补救措施: git checkout -b feature/xxx"
用户: [执行补救措施]
用户: "retry"
系统: "✅ 检查通过，开始执行步骤5"
```

---

## 🔗 相关文档

- [dev.md](./dev.md) - 主流程文档
- [dev-steps.md](./dev-steps.md) - 详细步骤说明
- [dev-rollback-guide.md](./dev-rollback-guide.md) - 回滚决策树
- [dev-checklist.md](./dev-checklist.md) - 执行检查清单

---

**文档结束**
